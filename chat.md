# 米老鼠与 xubin 的毕设协作对话全记录

## 项目：基于 Transformer 的音频降噪系统

---

### 一、项目启动（2026-05-07）

**用户**：我要进行音频降噪毕设，扫描当前目录代码。

**助手**：扫描发现项目是半成品，有基础 STFT 特征提取和简单模型。建议：下载数据集、搭建完整流水线。

**用户**：下载真实数据集，<10GB，越小越好。

**助手**：推荐 TIMIT + NOISEX-92（本地已有），共 1719 干净语音 + 16 种噪声 ≈ 280MB。

**用户**：数据在 data/clean/ 和 data/noise/，生成训练数据。

**助手**：实现 mix_audio.py（4 SNR 级别），生成 6876 对含噪-干净语音。拆分 2000 训练 / 100 测试。

**用户**：awesome-denoiser 参考项目有哪些可用的？Transformer 上有改进吗？

**助手**：分析后提出 3 个改进：
1. Conv1D 频率前端（捕捉谐波结构）
2. 因果注意力（支持流式推理）
3. 复数 IRM 掩膜（cIRM，同时修正幅度和相位）

**用户**：全部实施。一个一个任务推进。

**助手**：实现完整的 TransformerDenoiser 模型，ConvFrontend + 2 层 Transformer Encoder + 幅度/cIRM 双输出模式。训练基线模型 30 epochs。

**用户**：将规则写入配置文件。查看进度。

**助手**：训练完成（STFT 基线），追加 cIRM 模式代码，设置任务清单。

**用户**：改为全自动模式。

---

### 二、训练与实验（2026-05-09 - 2026-05-10）

**助手**：自动推进：
- Task 8：STFT + cIRM 训练 30 epochs（loss 0.0265）
- Task 9：Mel + cIRM 训练 30 epochs（loss 0.0341）  
- Task 10：RNNoise 风格 + cIRM 训练 30 epochs（loss 0.0484）

**用户**：每个 epoch 汇报。中断 Task 10。

**助手**：添加 --resume 断点续训功能。继续 Task 10。

**用户**（多次）：训练到哪了？

**助手**：持续监控，汇报每个 epoch。

**用户**：loss 回升代表什么？过拟合怎么解决？L2 正则化是什么？

**助手**：详细解释过拟合原理、L2 正则化（权重衰减）、数学公式。

**用户**：STOI 和 SDR 是什么指标？

**助手**：STOI 测可懂度（0-1），SDR 测信号保真度（dB），两者互补。

**用户**：项目讲解一遍，用专业话术。

**助手**：提供完整的答辩级技术讲解，涵盖问题定义、STFT 理论、cIRM 数学推导、ConvFrontend 动机、Transformer 架构、评估体系。

**用户**：中断 Task 10，明天继续。

---

### 三、cIRM Bug 与诊断（2026-05-10）

**助手**：续训 Task 10（--resume）。发现 >> 追加导致的缓冲问题，改为 > 覆盖。

**用户**：继续 Task 10。训练到哪了？

**助手**：Task 10 完成（RNNoise + cIRM，30 epochs）。自动启动 Task 5（STFT + cIRM + causal）。

**助手**：全部训练完成。跑批量推理和评估。

**关键发现**：所有 cIRM 模型 SDR 为负值！
- STFT + cIRM：SDR -1.61 dB
- Mel + cIRM：SDR -2.11 dB  
- RNNoise + cIRM：SDR -2.83 dB
- STFT + cIRM + causal：SDR -0.55 dB

**助手**：根因分析 — Tanh 激活将 cIRM 输出约束到 [-1,1]，但理想 cIRM 值无界。

**用户**：修复它。

**助手**：Tanh → Identity。重训 STFT + cIRM，21 epochs，单样本 SDR +16.70 dB（看似成功）。

**深入调试**：发现评估脚本有计算 bug（括号位置），实际 SDR 仍为负（-3.60）。根因更深：训练用幅度 L1 loss，但 cIRM 推理需完整复数谱重建，训练目标与推理需求不一致。

**用户**：记录数据，明天继续。

---

### 四、模型优化与论文（2026-05-11）

**用户**：继续任务。

**助手**：确认 cIRM 放弃。幅度掩膜基线已 STOI 0.807 / SDR 8.55。

**用户**：有没有优化方法提高 SDR？

**助手**：提出 3 个方案：
- Wiener 后处理（无需重训，10 分钟）
- 频域加权 L1 + 余弦退火 LR（需重训 3.5h）
- SI-SDR Loss 等高级方法

**用户**：先试 Wiener。

**助手**：实现 Wiener 后处理，全测试集评估 → SDR +0.29（边际无效），放弃。

**用户**：改造 RNNoise。

**助手**：重写 run_rnnoise_demo.py（内存直通，消除 16bit 量化损失）。但内存版有 bug（输出格式错误），文件版结果不变。

**用户**：频域加权 + 余弦退火方案。

**助手**：修改 train_transformer.py，加入：
1. 频域加权 L1 loss（低频 3× → 高频 1×）
2. 余弦退火 LR（1e-3 → 1e-6）

训练 30 epochs GPU（venv312 新环境）。

**结果**：STOI 0.884（+9.5%），SDR 12.67 dB（+48%）！

**用户**：翻译成论文。

**助手**：基于 毕业论文_正文.md，全面更新：
- 中英文摘要（加入实际数据）
- 第 3 章：新增 ConvFrontend、因果注意力完整小节
- 第 4 章：完全重写（真实数据集、4 种对照、2 个变体、全真指标）
- 第 5 章：完全重写（5 项贡献、4 条展望）
- 训练策略：双配置对比（基线 vs 改进版）

导出 毕业论文_完整版.docx（40 KB）。

**用户**：GPU 加速。

**助手**：检测 RTX 3050 4GB + CUDA 13.2。因 Python 3.14 无 CUDA wheel，降级到 3.12。
- 安装 Python 3.12.10 + PyTorch 2.6.0+cu124
- 创建 venv312/ 虚拟环境
- 训练加速 4.7×（30 epochs: 3.5h → 50min）

**用户**：继续优化。

**助手**：分析可提升方向，推荐 Griffin-Lim 相位精炼。

**用户**：试试。

**助手**：实现 Griffin-Lim 迭代相位恢复。单样本有效（+4.25 dB），但全测试集不稳定，放弃。

**用户**：结束任务，将重要内容写入配置文件。

---

### 五、最终成果

| 方法 | STOI | SDR (dB) |
|------|------|------|
| 含噪不处理 | 0.768 | +2.60 |
| RNNoise | 0.612 | −2.74 |
| GTCRN | 0.713 | +0.14 |
| noisereduce | 0.783 | +4.24 |
| Transformer 基线 | 0.807 | +8.55 |
| **Transformer 改进版** | **0.884** | **+12.67** |

**交付物**：
- 毕业论文_正文.md（源码）
- 毕业论文_完整版.docx（导出）  
- 所有代码、checkpoint、图表
- GPU 训练环境（venv312）
- 对话全记录（chat.md）
